[package]
name = "lau-construct"
version = "0.1.0"
edition = "2021"
description = "The Matrix Construct — a shared creative space where user and agent co-create PLATO environments"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
